import event from './event';
import producer from './producer';
import siteSettings from './siteSettings';
import venue from './venue';

export const schemaTypes = [siteSettings, event, venue, producer];
